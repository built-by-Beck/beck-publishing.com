# Firebase App Hosting runtime config
# Docs: https://firebase.google.com/docs/app-hosting/configure
runConfig:
  # Scale to zero when idle so there's no standing cost.
  minInstances: 0
  # Cap concurrent instances to keep spend predictable.
  maxInstances: 2
  concurrency: 80
  cpu: 1
  memoryMiB: 512
